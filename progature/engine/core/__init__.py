"""`core` module is main part of the app.
This module is responsible for working with `Game` objects.

`core` module is an interface for app to work with `Game` objects.
writing and changing and managing `Game` objects are implemented here.


Notes
-----
we have `game` submodule that contains<br>
1. handler<br>
2. manager<br>
3. loader<br>
"""